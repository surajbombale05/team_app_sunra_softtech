import 'package:flutter/widgets.dart';

class PendingPaymnetsWidget extends StatelessWidget {
  const PendingPaymnetsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Pending payments"),);
  }
}